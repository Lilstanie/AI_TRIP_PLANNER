// Owner: E. The home page is intentionally empty: planning starts only after
// the traveller sends their first message.
import { Workspace } from "@/components/Workspace";

export default function Page() {
  return <Workspace />;
}
